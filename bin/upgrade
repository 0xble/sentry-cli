#!/bin/sh
set -e
cd "$(dirname "$0")/.."
cargo build --release
cp target/release/sentry-cli ~/.local/bin/sentry-cli
echo "installed sentry-cli from $(git describe --tags --always 2>/dev/null || git rev-parse --short HEAD)"
